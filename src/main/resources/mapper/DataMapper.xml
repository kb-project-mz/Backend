<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="fingertips.backend.data.mapper.DataMapper">

    <insert id="insertData" parameterType="DataDTO">
        INSERT INTO member (member_id, password, member_name, birthday,
                                         gender, email, is_active, join_date, role, is_login_locked, login_lock_time)
        VALUES (#{memberId}, #{password}, #{memberName}, #{birthday}, #{gender}, #{email}, #{isActive},
                    #{joinDate}, #{role}, #{isLoginLocked}, #{loginLockTime})
    </insert>

</mapper>
